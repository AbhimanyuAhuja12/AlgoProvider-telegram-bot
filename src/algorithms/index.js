const searching = require('./searching');
const sorting = require('./sorting');

module.exports = {
    sendSearchingAlgorithms: searching.sendSearchingAlgorithms,
    sendSortingAlgorithms: sorting.sendSortingAlgorithms
};
